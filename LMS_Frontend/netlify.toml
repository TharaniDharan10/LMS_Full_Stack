[build]
  # Sets the working directory for all commands
  base = "LMS_Frontend" 

  # Runs the standard build command inside LMS_Frontend
  command = "npm run build"

  # Specifies the folder to publish, relative to the base directory
  publish = "build"